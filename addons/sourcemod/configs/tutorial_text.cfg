"tutorial_text"
{
    "testmessageId"
    {
        "text"  "ho!"

        "ko"
        {
            "text"  "호!"
        }

        "cookie_rule"       "0"
        // 0 (Type_Normal) = text is no longer ignore rules. (But first message is will ignore SETTING.)
        // 1 (Type_OnlyOne) =  only show when first time. (SETTING ignored.)
        // 2 (Type_EveryTime) = if text following this rule, always show text. (SETTING ignored.)
        // 3 (Type_NormalEvenFirst) = text is no longer ignore rules.

        "show_effect"       "1"
        "show_distance"     "1"
        "id"                "1"    // custom_id for hide annotation.
        "lifetime"          "10.0"
        // "play_sound"        ""   // Type sound path
    }

    "this_is_text_text"
    {
        "text"  "This is Test Text"

        "ko"
        {
            "text"  "이것은 테스트 텍스트입니다."
        }

        "cookie_rule"       "0"
        // 0 (Type_Normal) = text is no longer ignore rules. (But first message is will ignore SETTING.)
        // 1 (Type_OnlyOne) =  only show when first time. (SETTING ignored.)
        // 2 (Type_EveryTime) = if text following this rule, always show text. (SETTING ignored.)
        // 3 (Type_NormalEvenFirst) = text is no longer ignore rules.

        "show_effect"       "1"
        "show_distance"     "1"
        "id"                "2"    // custom_id for hide annotation.
        "lifetime"          "10.0"
        // "play_sound"        ""   // Type sound path
    }
}
